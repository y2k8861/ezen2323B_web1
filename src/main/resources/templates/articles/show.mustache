{{>layouts/header}}  <!-- 헤더 파일 호출 -->
    {{name}}
    <table border="1">
        <thead>
            <tr>
                <th>ID</th>
                <th>Title</th>
                <th>Content</th>
            </tr>
        </thead>

        <tbody>
            <tr>
                <th>{{article.id}}</th>
                <th>{{article.title}}</th>
                <th>{{article.content}}</th>
            </tr>
        </tbody>
    </table>
<a href="/articles">목록</a>
<a href="/articles/{{article.id}}/edit">수정(Update)</a>
<a href="/articles/{{article.id}}/delete">삭제(Delete)</a>
{{>layouts/footer}}  <!-- 푸터 파일 호출 -->